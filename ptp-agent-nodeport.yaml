apiVersion: v1
kind: Service
metadata:
  name: ptp-agent-nodeport
  namespace: ptp-agent
  labels:
    app: ptp-agent
spec:
  type: NodePort
  selector:
    app: ptp-agent
  ports:
  - name: mcp-integration
    port: 8081
    targetPort: 8081
    nodePort: 30081  # Choose an available port 30000-32767
